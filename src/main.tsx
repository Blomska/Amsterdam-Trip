import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './amsterdam_reiseportal'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
